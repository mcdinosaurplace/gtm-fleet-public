# AGENTS.md

This repository's agent instructions live in **[CLAUDE.md](CLAUDE.md)** (the canonical
file for Claude Code) — registry, folder map, documentation index, git workflow, and
coding principles. Codex and other agent runtimes should read that file; the agent
roster itself is under `roster/<agent>/prompt.md`, and the user-facing entry points are
the plugin skills under `skills/<name>/SKILL.md` (`/gtm-fleet:<name>`).
