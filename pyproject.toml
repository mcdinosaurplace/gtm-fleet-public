[project]
name = "gtm-agent-fleet"
version = "0.1.0"
description = "A portable GTM / marketing-operations agent fleet for Claude Code (plugin gtm-fleet)."
requires-python = ">=3.10"
license = {text = "Apache-2.0"}

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --import-mode=importlib"
